import { QuizList } from '@/features/quiz-list';
import { Container } from '@/widgets/container';

export const QuizListPage = () => {
  return (
    <Container>
      <QuizList />
    </Container>
  );
};

export const Component = QuizListPage;
